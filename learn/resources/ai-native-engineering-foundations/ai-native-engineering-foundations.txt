Becoming an AI‑native engineer
Selecting transcript lines in this section will navigate to timestamp in the video
AI coding agents are transforming how software gets built. But here's what most people get wrong. Using them effectively takes real skill. It's not a push-button magic experience. This course gives you the practical foundation to get productive with AI, covering the mindset, techniques, and professional workflows used daily. I'm Addy Osmani. I've spent 14 years building developer tools at Google, and I've been helping engineering teams adopt AI-assisted coding workflows. The tools in this space evolve fast, but the principles transfer. If you're ready to build a real AI coding workflow, let's get started.
Vibe coding vs. agentic engineering
Selecting transcript lines in this section will navigate to timestamp in the video
Let's start with a distinction that'll shape everything else in this course. There are two fundamentally different ways people use AI for coding. Let's call them vibe coding and agentic engineering. Understanding the difference and when to use each is the first real skill. Now, vibe coding is the intuitive approach. You describe what you want in natural language and the AI generates the code. You just run it. If it works, great. If not, you describe the problem back to the AI and you iterate. It's fast, it's creative, and it's perfect for prototyping. Here, let me show you what this looks like in practice. So I'm going to open up VS Code, and I'm going to ask it to build me something real quickly. I'll type into the chat, build me a React component that displays a countdown timer with start, pause, and reset buttons. So I'm now going to hit enter, and this is just going to send that prompt to the model. It's going to think through the problem and how to structure this inside the project. So in the thinking here, we can see it's defining our timer parameters. It's implementing some event handlers. I always love to just read through the thinking because it gives me a sense of how this problem is being solved. So it's just finishing up that countdown timer. We go inside here soon and we'll be able to take a look at the actual code that is generated. So here it's summarized that it's completed that task. Amazing, so this is our countdown timer. We've got all the code for it generated. It's working great. This is vibe coding. I described the vibe and the AI delivered. For a prototype or a personal project, this is really useful. But here's where it gets even more interesting. If we take a look at the code that was generated, you can notice that maybe there's a few opportunities this could be better. Okay, so if I look closely at this code, I can spot a few issues. There's no cleanup on unmount, and this interval will keep running. That could cause a memory leak. If we keep scrolling through, state management looks like it could be cleaner, and we also don't have accessibility markup here. Now, if I ask the AI to fix one thing, it might break another. This is what I call the two steps forward, one step back pattern. You fix a bug, that fix creates another bug, you fix that, and another appears. Non-engineers hit a wall here. Experienced developers know how to break through it. Agentic engineering is the disciplined approach. You still use AI aggressively, but you stay accountable for the code. You start with a clear specification. You break work into small focused tasks. You review every line the AI generates. Maybe you lean on tests to verify behavior and you commit often so you can roll back. The AI becomes a fast pair programmer, but you are the senior engineer directing the work. Here's when to use each approach. Now, vibe coding works for rapid prototypes and MVPs, so if you have personal projects and experiments, it's great for that. Really good for exploring an idea quickly or learning a new framework. agentic engineering is for production code your team maintains. So anything that users are gonna depend on, code that needs to be secure and high performance, anything that's going to live a long time. Now, most developers need both modes. The skill is knowing which you're in and being intentional about it. Now, if you want a pro tip, if you wouldn't merge the code without a review, you're in engineering mode. If you throw it away tomorrow, vibe coding is totally fine.
The 70% problem: Understanding AI's role
Selecting transcript lines in this section will navigate to timestamp in the video
Here's a pattern I've observed across dozens of engineering teams. AI can get you about 70% of the way to a working solution surprisingly quickly, but that last 30%, that part really makes software production ready. And that's where human engineering skill becomes essential. I call this the the 70% problem, and understanding it changes how you approach AI-assisted development. Let's break down what AI handles well and where it struggles. AI excels at the routine, well-documented parts of development. So think your boilerplate and scaffolding, where maybe you're setting up a project, you've got your config files, your basic CRUD. Then you've got your patterns, so standard design patterns, your typical UI components, those common algorithms. You've got code transformation, so converting between different languages, refactoring. You've got your documentation and your tests, so generating docs, your basic unit tests, your README files. And then common integrations, so API calls, authentication, database queries, basically standard patterns. These are tasks where the patterns are well-established and there's abundant training data. The AI has seen thousands of examples and can reproduce them pretty reliably. Now here's where you come in. 30% that still requires human judgment. You've got your architecture decisions. How should components interact? What patterns fit into this specific system? You've got your edge cases. What happens when the network drops or when the user enters in something unexpected? You've got security. Is the code vulnerable to injection or are you leaking sensitive data? Performance optimization. Is the approach scalable? Is the quality good? You've got product understanding. Does it actually solve the user's problem? This isn't a limitation that's gonna go away soon. These are areas where context, experience, and judgment matter the most. Now here's the counterintuitive part. It's what I call the knowledge paradox. AI tools help experienced developers more than beginners. Think about it. When a senior engineer uses AI, they can usually spot incorrect suggestions immediately. They can guide the AI toward the right approach. They can catch those security and performance issues in generated code. And they know what parts to accept and which ones to reject. When a less experienced developer uses the same tools, they might accept incorrect or outdated patterns. They might miss those security vulnerabilities and they might build a fragile system that they don't really fully understand or get stuck in fixed break cycles. AI is like having a very fast, very eager junior developer on your team. They can write code quickly, but they need supervision. The more you know, the better you can direct them. So here's the practical takeaway. Use AI to accelerate what you already understand and use it as a learning tool for areas that you're growing in. Let it handle the 70% routine work so you can focus your energy on the 30% that requires real engineering judgment. Don't expect AI to replace the need for understanding. Instead, think of it as freeing up your cognitive bandwidth for the hard problems. Now, if you want a pro tip, when AI generates code, ask yourself, could I do a code review for this? If you can't evaluate the output, you need to build your understanding of that area first. And AI can actually help you learn it as long as you're actively studying, not just copying.
Human-AI collaboration principles
Selecting transcript lines in this section will navigate to timestamp in the video
Now that we understand the spectrum and the 70% problem, let's establish the principles that guide effective human-AI collaboration. These apply regardless of which tool you use. Principle one, you own the code. When AI generates code and you ship it, it's your code now. You're accountable for its correctness, its security, and its performance. This isn't about distrust, but professionalism. You wouldn't ship a colleague's code without reviewing it. Apply the same standard here. At Anthropic, engineers adopted Claude Code so heavily that about 90 percent of the code for Claude Code itself was written by Claude Code. But every line still went through some sort of review. The AI wrote code faster, but humans own the quality. Principle two, review and iterate. Don't just accept and move on. The biggest mistake I see developers make with AI tools is accepting the first output without scrutiny. AI code looks convincing. It's syntactically correct, it uses reasonable variable names, and it often runs on the first try. But it runs is not the same as it's correct. Think of it this way, AI is an overconfident, tireless pair programmer. It writes code with complete conviction, including the bugs. It won't tell you something is wrong unless you catch it. Your workflow should be request, review, refine, and accept. Every time. Principle three, AI amplifies your skills. It doesn't replace them. Here's something that I find exciting rather than threatening. Every skill that makes somebody a strong engineer, designing systems, managing complexity, knowing what to automate versus what to hand code, those skills become even more valuable when you're working with AI. You're not being replaced, you're being given a faster horse. The developers who get the most out of AI are the ones who are already solid engineers. They use AI to prototype ideas in minutes instead of hours. They explore alternative approaches rapidly. They automate the tedious parts so they can focus on the creative ones. Let me show you this in practice. Here's a quick example. So I have a function here that processes user data. Let me ask AI to review it. So we have the function, it's processUserData. And I'm just going to select the body of this function. And in VS Code, you'll see that in the chat, it's already flagged the different ranges for this. I've asked to review the code for potential issues. And now it's thinking, it's analyzing this function. We see that there's some naming, and it's flagged a few potential issues here. You'll see no error handling for async operations, null undefined on the user object, null undefined on orders, and missing data validation for amount. So the AI identified a few issues. And these included, you know, no error handling for async operations, null undefined user object and missing data validation. Now I can accept all of these suggestions, but notice that some of these suggestions could change the control flow for the way that this problem is being solved. And that's where my judgment comes in. Maybe I'll accept only some of the changes and then handle the others in a slightly different way than what it suggested. Let me summarize the mindset with three rules. You are the senior engineer. AI is your fast, eager assistant. Review everything. Never ship code you can't explain. And use AI to level up. Let it handle the routine. You focus on what matters. These principles will guide everything else that we cover in the course. With the right mindset, AI becomes an incredible force multiplier. Without it, you're building a house of cards. Now here's a pro tip. Simon Willison, one of the most experienced AI-assisted developers, frames it perfectly. Think of an LLM pair programmer as overconfident and prone to mistakes. Internalize that framing. It'll save you hours of debugging.
Deep Dive: LLM Fundamentals for Developers
How AI coding models work—and why they fail the way they do
To use AI coding tools effectively, it helps to understand what's happening under the hood. You don't need a PhD in machine learning, but knowing the basics of how large language models (LLMs) work explains both their impressive capabilities and their characteristic failure modes.
What is an LLM?
Large language models are neural networks trained on vast amounts of text data — including billions of lines of code. When you give an LLM a prompt, it predicts the most likely next tokens (words or code fragments) based on patterns it learned during training.
Key insight: LLMs don't "understand" code the way you do. They recognize patterns and generate statistically likely continuations. This is why they can produce syntactically correct code that's logically wrong—the pattern looks right even when the logic isn't.
How code generation works
When you ask an AI to write code, here's what happens:
Your prompt is tokenized—broken into small pieces the model can process.
The model considers all the context it has: your prompt, any files you've provided, system instructions, and conversation history.
It generates tokens one at a time, each one being the statistically most likely next token given everything before it.
The output is assembled into the code you see.
This explains several behaviors:
Why AI code looks convincing—it follows the statistical patterns of well-written code.
Why specificity matters—more context narrows the probability space to better outputs.
Why AI "hallucinates"—it generates plausible-looking but nonexistent API calls because the pattern matches even when the specific function doesn't exist.
The context window
Every LLM has a context window — the maximum amount of text it can "see" at once. Modern coding models typically have windows of 100K-200K tokens (roughly 75,000-150,000 words). This context window includes:
System instructions (from the tool)
Your conversation history
Any files or code you've provided as context
The AI's own previous responses
When the conversation gets too long, older context gets "pushed out" of the window. This is why starting fresh conversations for new tasks often produces better results than continuing very long threads.
Why AI code fails
Understanding the mechanism helps you predict and catch failures:
Hallucinated APIs: The model has seen patterns like library.method(args) thousands of times. It can generate a method call that looks right but doesn't exist, because the pattern is statistically common even if that specific method isn't real.
Training data cutoff: Models are trained on data up to a certain date. They don't know about APIs released after that date, and they may suggest deprecated patterns from older library versions.
Confident incorrectness: The token-by-token generation process doesn't have a "I'm not sure" mechanism. The model generates the most likely next token regardless of whether it's correct.
Context sensitivity: The same prompt with different surrounding context can produce very different outputs, because the statistical patterns shift based on what the model "sees."
Models used in coding tools
Different AI coding tools use different underlying models:
Claude (Anthropic) — Used in Claude Code, Cursor, and others. Known for strong reasoning and long context windows.
GPT-4 / GPT-4o (OpenAI) — Used in GitHub Copilot and ChatGPT. Strong general-purpose coding.
Gemini (Google) — Used in Gemini CLI and Google tools. Good integration with Google ecosystem.
DeepSeek, Qwen, and others — Open-weight models gaining ground in coding tasks.
The choice of model matters less than how you use it. The techniques in this course — specific prompts, rich context, review-iterate workflows — improve results across all models.
Key takeaways
LLMs predict probable code; they don't reason about correctness.
More context = better predictions = better code.
Hallucinations aren't bugs — they're a natural consequence of pattern-matching.
Understanding these mechanics makes you better at directing AI and catching its mistakes.
The 70% Problem: Hard Truths About AI-Assisted Coding
What happens after AI gets you most of the way there
There's a pattern that emerges consistently when developers adopt AI coding tools: the AI gets you about 70% of the way to a working solution surprisingly fast. The remaining 30% — making it production-ready — is where the real engineering happens.
The pattern in practice
A typical AI-assisted development session looks like thiås:
You describe what you want. The AI generates a first draft in seconds.
The code looks good. It compiles, it runs, basic tests pass.
You start looking closer. Edge cases are missing. The error handling is generic. There's a potential security issue.
You iterate. Each fix introduces new complexity. Some AI suggestions help; others create new problems.
Eventually, you're doing careful, manual engineering work to get the last 30% right.
This isn't a criticism of AI tools — it's a recognition of where they excel and where they don't.
What the 70% includes
Boilerplate and project scaffolding
Standard patterns (CRUD endpoints, form components, data transformations)
Common algorithms and data structures
Basic test generation
Documentation and comments
Code transformations and refactoring
What the 30% requires
Architectural decisions that fit your specific system
Edge case handling for your specific domain
Security hardening (input validation, authentication, authorization)
Performance optimization for your specific scale
Integration with existing systems and conventions
Product judgment—does this actually solve the user's problem?
The knowledge paradox
Here's the counterintuitive finding: senior engineers get more value from AI tools than junior engineers. This is the "knowledge paradox."
Senior engineers can evaluate AI output immediately, spot hallucinated APIs, catch security issues, and guide the AI toward better solutions. They use AI to accelerate work they already know how to do. 
Less experienced developers may accept incorrect patterns, miss vulnerabilities, and build systems they don't fully understand. The AI's confident output can mask a lack of foundational knowledge.
The implication: AI tools are a multiplier, not a replacement for skill. Invest in your engineering fundamentals, and AI tools will amplify that investment.
Working with the 70/30 split
The most effective approach is to embrace the split:
Let AI handle the 70%—generation, boilerplate, first drafts, common patterns.
Focus your energy on the 30%—architecture, security, edge cases, product decisions.
Use the review-iterate workflow to bridge the gap systematically.
Build your engineering skills—they become more valuable, not less, in an AI-assisted world.
Career Guide: Future-Proofing Your Engineering Skills
How AI changes what's valued in engineering—and how to position yourself
AI coding tools are reshaping what it means to be a productive software engineer. Some skills are becoming less differentiating (memorizing syntax, writing boilerplate), while others are becoming more valuable than ever.
Skills that are becoming more valuable
System design and architecture—AI can generate code but can't design systems. Understanding how components interact, choosing the right patterns, and making scalability decisions are increasingly important.
Code review and quality judgment—The ability to evaluate AI-generated code quickly and accurately is a core skill. This requires a deep understanding of your domain.
Specification writing—The better you can describe what you want, the better AI output you'll get. Clear thinking translates to clear specs translates to better code.
Security awareness—AI often generates code that works but isn't secure. Understanding OWASP top 10, authentication patterns, and data protection makes you invaluable.
Context engineering—Knowing what information to provide to AI tools, and how to structure it, directly impacts output quality.
Problem decomposition—Breaking complex problems into small, well-defined pieces is more important than ever. Each piece becomes a clean AI prompt.
Skills that are less differentiating
Memorizing syntax and API details—AI tools handle this well.
Writing boilerplate code—Automation handles the repetitive parts.
Speed of typing—Output velocity matters less when AI generates most of the code.
Building your AI-native skill set
Master your fundamentals—Data structures, algorithms, design patterns. These let you evaluate AI output effectively.
Learn to write specifications—Practice describing features clearly with requirements, constraints, and acceptance criteria.
Develop security intuition—Study OWASP, practice code review, learn to spot common vulnerabilities.
Build your context engineering muscle—Practice providing the right files, constraints, and examples to AI tools.
Stay tool-agnostic—Learn the principles (prompting, context, review) rather than memorizing tool-specific features.
Embrace continuous learning—The tools evolve fast. Build a habit of experimenting with new capabilities regularly.
The bottom line
AI doesn't replace engineers—it amplifies them. Engineers who embrace AI tools while strengthening their judgment, design, and communication skills will be significantly more productive than those who resist or those who rely on AI without understanding. The future belongs to engineers who can direct AI effectively while maintaining high standards of quality, security, and craftsmanship.
Editors: AI-enhanced IDEs
Selecting transcript lines in this section will navigate to timestamp in the video
The most popular way developers use AI coding today is through AI-enhanced editors and tools. I'm going to walk you through the key features using VS Code, but I'll point out where these features map to other AI-enhanced editors like Cursor, Windsurf, and Antigravity. Here's VS Code. If you're using GitHub Copilot, it's built right in. You can also install third-party plugins like Cline, which allow you to bring your own API keys. So let me show you three core features that you'll use constantly. The first is inline code completion. So I'm going to start typing a function, validate e-mail, and watch what happens. Before I type anything else, the AI is already suggesting how to complete this function. These are ghost text suggestions, and they appear in gray as you type. So I can press tab to accept, and the AI completed the entire function, including the regex pattern. This is your most basic interaction, AI predicting what you're about to write. This is similar to GitHub Copilot's inline suggestions in VS Code. As a pro tip, you don't have to accept the entire suggestion. In VS Code with Copilot, you can use command-right-arrow to accept this word by word. See that? We're flipping through different tokens, different words, and we're able to accept these changes one by one if we'd like. This gives you more control over what gets inserted. Feature two is the chat panel. Now let me show you the chat panel in the preview here. So I've got the chat open, and this is where you have conversations with the AI about your code. Let me select this function, and I'm just gonna ask it some questions. So add input sanitization to this function, add handling for edge cases like empty strings, null values, and strings that are too long. Now, what the AI is gonna do is think through how to approach this problem, and when it's done, it will show me a diff with the changes that it wants to make. So it's analyzing the file content right now. And here's our diff. I can see exactly what's being added and modified before I accept anything. And this is critical. Always review the diff before you're applying changes. In VS Code with GitHub Copilot, this is the Copilot chat panel. And it supports multiple built-in agent modes as well. So for example, here you see Agent, Ask, and Plan. Use Agent for complex coding tasks, plan for creating structured implementation plans, and ask for answering questions about your code base. Now the interface for this might look a little bit different in other editors, but the core idea is the same. You basically select code, describe what you want, and review the changes. Next, let's talk about Agent Mode. This is the most powerful feature. Agent Mode lets the AI make changes across multiple files, run commands, and execute multi-step tasks autonomously. And I'm gonna give it a more complex task this time. I wanna create an Express API endpoint that validates emails using the function we just wrote. We're gonna include proper error handling, a test file using Jest, and update our package.json with the necessary dependencies. So watch what happens. I've typed in my prompt. I'm going to hit Enter now. And so the model will then start to process this. And what's going to happen is it's going to just think through how to approach this problem. So it's defining that API endpoint for us. It's reading our package.json. Again, I love reading through the thinking because it just gives me a sense of how the architecture is being approached, how it's solving the problem. So it's refining our server structure here. Occasionally, what you will see is that the model might ask for permission to run a command. This is actually a really good thing. You don't want destructive commands run on your behalf without asking you. So that's exactly what it does. And you actually have a lot of flexibility here. You can tell the model, hey, I'm okay with you running these commands without my input, or maybe you actually prefer to manually allow these things to run. So in this case, again, there's a command that it wants to run. npm install, we use for installing our dependencies. We can either just have it, you know, go and run this once, or we can allow all. And so in this case, let's go ahead and let that command run so we can install our dependencies and continue moving on with our task here. I wanted to show you the enable auto approve. You'll just get that prompt that pops up real quickly and you can either allow it to always do the npm install or all the commands that it might need to be successful here. So here what we can see is that it's running our npm tasks. We're getting close to being final here. It's working across multiple files simultaneously. This is the shift from AI suggests code to AI executes tasks. Now, if we take a look, we're almost done. It looks like it's confirmed successful execution. The implementation itself seems like it's working. Initial tasks are all passing and amazing. It's created our server. We have an express server configured with our post, our validate endpoint, and we've got tests that are passing. So in VS Code with Copilot, this is agent mode. It now supports local, background, cloud, and third-party agents, plus the ability to hand off sessions between different agent types. In other editors, a lot of this functionality is also supported, so check out the relevant documentation. Now, I want to be clear. I would still review all of this before committing. The agent did a lot of work quickly, but I need to verify that the error handling is correct, that the tests covered the right cases and dependencies are appropriate. To summarize, every major AI enhanced editor gives you three modes. Inline completion, so ghost text as you type, that's fast for small edits. You have your chat panel, so conversation-based editing, best for questions, explanations, and targeted changes where you're supplying it a prompt. And then agent mode, which is autonomous multi-file changes, best for larger tasks. I'm using VS Code, but other AI enhanced editors like Cursor, Windsurf, and Antigravity all provide similar modes. So pick the one that works best for your team and your workflow. As a pro tip, the fastest productivity gain comes from really learning the keyboard shortcuts to your chosen editor's AI features. In Cursor, tab for completion, command L for chat, command I for your agent. In VS Code with Copilot, tab for completion, command I for inline chat and the chat view in the sidebar. Muscle memory with these shortcuts will save you significant time.
Command-line AI tools
Selecting transcript lines in this section will navigate to timestamp in the video
Not everything happens in an editor. If you spend a lot of time in the terminal, and many developers do, command line AI tools are a game changer. Let me show you Claude Code, which runs right in your terminal. So first, we're gonna make sure that we have Claude Code installed. Now there are a lot of different ways that you can do this. You can npm install it, but check out the documentation for your platform. So I've got it installed. I'm gonna navigate to a project directory and just type in Claude. That's it, I'm now in an interactive AI session that has full access to my project files. Now, this is fundamentally different from chatting with AI in a web browser. Claude Code can read your entire codebase, run commands, write files, and execute tests. So let me show you what this looks like in practice. I'm in a project right now. I'm gonna ask it, what does this project do? Give me a brief overview of the architecture and key files. So watch, it's reading through the files, It's gonna analyze the structure and give me a summary. If it asks you for permission, just make sure to go ahead and say yes. So it's searching for different patterns right now, and it's gonna give us a summary at the very end of this with that architecture in the key files, something that would often take us a long time to do ourselves. So here we go. It tells us exactly what this application is. It's a REST API with hard-coded endpoints for managing items. It mentions the key files here as well, and it also mentions some additional notes that it found. This is incredibly useful when you're jumping into an unfamiliar codebase. Instead of spending an hour reading through files, you get an informed summary in seconds. Now, let me give it a real task. Add a health check endpoint to the Express server in server.ts and should return the app version from package.json and the current uptime. Write a test for it too. So let's take a look at what happens this time. It's thinking, it's starting to generate some code diffs and you can see those diffs directly inside the terminal as well. So this is working through that API health endpoint. So it's going to read the server.ts file to understand the patterns. It's going to read our package.json file for the version information. It's going to write the new endpoint following our code style, create a new test file, and then run the tests as well. Similar to before, if you see it asking you for permissions to do something, just go ahead and allow it, read, of course, what it's asking you for permissions to do. And if it runs into any bugs, that's working as intended. It will just keep working through until it gets to a happy place. I would recommend reading this output, but you're basically getting to see the full loop here. And this is all happening directly inside of your terminal. You get to see every step, approve every action, and the whole thing integrates with your existing workflow. So we're almost there. We're just working through a few small hiccups that you'd normally run into when building out a project. So it's noting that there's some modules that it still needs to sort out. It's still combobulating, but now we have all of our tests passing and here's where we landed. Everything's working great. Our health check describes a block verifying our status code, our version string, uptime, and our null error. Here's a pro tip. When you first start Claude Code in a project, create a claude.md file in the root. This is like an onboarding doc for the AI. put your project's architecture, your coding standards and common commands in it, and Claude Code will automatically make sure it gets read every session. Let me also show you one more powerful feature. I can actually pipe things directly into Claude Code. So I typed in git diff Claude review these changes for potential issues. And it's actually passed that diff right over to Claude. This lets you integrate AI into any terminal workflow. So your code review, your log analysis, your debugging, you're not limited to an IDE window. Claude Code is what I use, but there are lots of solid alternatives. Gemini CLI is Google's terminal AI tool, with similar file reading and task execution capabilities. There's the Copilot CLI from GitHub, integrated with your GitHub account, and good for Git-related workflows. There's Codex CLI, that's OpenAI's terminal tool. Each of these have different strengths, but the workflow is very similar. You navigate to your project, you start the tool, and you give it tasks. Now another pro tip, one of the most underused features of Claude Code is the plan command. Before tackling a complex task, type plan and describe what you want. Claude will create a detailed plan, the files to modify, the steps to take, potential risks, without making any changes. Review the plan, adjust it, and tell Claude to execute. This plan before code approach dramatically improves your results.
Choosing your modality
Selecting transcript lines in this section will navigate to timestamp in the video
You've now seen two different modalities, an AI-enhanced editor and a command-line tool. How do you choose between them? Well, let me give you a simple decision framework. There are three main modalities for AI system development. There's editor-based, where maybe you're using VS Code and Copilot, Cline, Cursor, Windsurf, Antigravity, an IDE. You've got your command line, so Claude Code, Gemini CLI, the Codex CLI. And you've also got cloud or background agents. So GitHub Copilot Coding Agent, Jules, Claude Code Web. Each of these things has got strengths for different situations. Use an AI-enhanced editor when you're doing interactive development. So writing features that require nuance or complexity, fixing bugs or refactoring. Or when you want to see code changes in real time with syntax highlighting and diffing. Or when you need the full IDE experience, So you're debugging, you've got extensions, an integrated terminal, and you're working in a visual code base. Maybe it's front end, maybe it's UI components. This is your default for a lot of day-to-day work. Use a CLI tool when you're already in the terminal and you want to stay there. Maybe you're doing ops type work. So scripts, deployments, system tasks. Maybe you need to integrate AI into a pipeline or a workflow. Maybe you're working on a remote server or you want the most direct control over what the AI sees and does. CLI tools also work well for quick one-off tasks where opening an editor would be a little overkill. Now, for cloud and background agents, use them when you have a well-defined task that you can hand off completely. Maybe you want work done in the background while you're doing something else. Maybe the task is large enough that autonomous execution makes sense. Maybe you're comfortable reviewing a pull request rather than watching the code get written. Now, these are newer and a little less mature, but they're really where things are headed. In practice, most developers use a combination. So here's my typical day. Maybe I start off by opening up VS Code and start working on the main feature that I'm building. I could use agent mode for multi-file changes and chat for targeted edits. Maybe I then switch over to Claude Code in the terminal for code reviews for debugging and exploring an unfamiliar part of the code base. And then I fire off a cloud agent to generate tests for the code I wrote today while I work on documentation. You don't have to pick just one. The best approach is to get comfortable with at least two modalities so you can match the tool to the right task. Pro tip, don't try to learn every tool at once. Pick one editor tool and one CLI tool. Get proficient with those two, and then expand. For most developers starting today, I'd suggest something like VS Code or Cursor plus Claude Code or Gemini CLI. That combination covers the vast majority of use cases already.
Cloud agents: Running AI in a sandbox
Selecting transcript lines in this section will navigate to timestamp in the video
So far, we've looked at AI in your editor and AI in your terminal. There's a third modality that's growing fast, Cloud-based coding agents or background agents. The idea is simple. Instead of running an AI agent on your local machine, where it has access to your files, your environment, and your credentials, you run it in a remote container, a sandbox. The agent gets its own isolated environment, clones your repo, does its work, and pushes the results. Let me show you Claude Code on the web. Now tools like Claude Code on the web, GitHub Copilot Agent, and Google's Jules all follow this pattern. You describe a task and the agent works in a cloud environment. You review the output, usually as a pull request or a diff. Now I want to show you this here. For example, I can add in a prompt to this app saying write me a dark mode toggle for my app. I can then submit it to the repository and the Claude agent will go and implement it. Similarly, you have a similar interface for GitHub Copilot's agent. You just have a prompt box. There are apps like Conductor for Mac and even Google's Jules that all follow the similar pattern of letting you type in a prompt and handing it off to a background agent. Now, why does this matter? Safety. When an agent runs locally, a bad command can delete files, leak secrets, or mess up your environment. Whereas in a sandbox, the worst it can do is just waste compute. Now, Simon Willison, one of the most prolific users of AI coding tools, has switched almost entirely to cloud-based agents for this reason. And I've done the same. This is great for async workflows. Cloud agents don't need your laptop open. You can kick off a task from your phone, go for a walk, and come back to a PR. This changes how you structure your workday. Instead of sitting with the agent through every iteration, you describe the task well, and you just let it run. Now here is my mental model for local versus cloud agents. I tend to like using local when I really require manual edits to be made, when a task has a lot of nuance or complexity to it. I like to defer to a background agent or a cloud agent when I have well-isolated work, when there's a feature in an app or a particular piece of functionality that is isolated enough that I can just hand that off while I continue working locally on other kinds of tasks. This is a force multiplier for my productivity. Notice that last row. Cloud agents force you to write better specs because you can't always course correct in real time. That's actually a feature. It builds the spec writing muscle that makes you better with all AI tools. Now for cloud-based agents, I personally really like using Claude Code on the web and GitHub Copilot Coding Agent, but there are plenty of other great options. Google's Jules, OpenAI's Codex on the web, Cursor's background agents, Conductor for Mac, Vibe Kanban, all of them offer variations on this pattern. You don't have to choose one modality exclusively. Most experienced AI-assisted developers use a mix, local agents for interactive exploration and debugging, cloud agents for well-scoped feature work and tasks that you want to run overnight. The trend is clear. Move more of the heavy lifting to cloud sandboxes, the developers who learn to work asynchronously with agents, writing clear specs and reviewing output are going to have a significant advantage. Here's a pro tip. If you're new to cloud agents, start with a small, well-defined task, like add a health check endpoint or write tests for this module. Give the agent a clear spec and review the pull request it produces. This builds your intuition for what makes a good async task description before you try handing off larger features.
Reference: AI Coding Tools Comparison 2026
VS Code + Copilot/Cline, Cursor, Windsurf, Claude Code, and more—features, pricing, and trade-offs
The AI coding tools landscape evolves rapidly. This reference compares the major tools available as of early 2026 to help you make an informed choice.
AI-enhanced editors
Cursor
Built on VS Code—all extensions and themes work
Three AI modes: inline completion (Tab), chat panel (Cmd+L), agent mode (Cmd+I)
Supports multiple AI models (Claude, GPT-4, Gemini)
Strong context handling with @-mentions and .cursor/rules
Pricing: Free tier available; Pro plan for more usage
VS Code + GitHub Copilot
Native integration with the world's most popular editor
Agent, Plan, and Ask modes in the Chat view
Supports local, background, cloud, and third-party agents
Custom agents via .agent.md files
Copilot coding agent for autonomous PR generation from issues
Pricing: Free tier for individual developers; paid plans for teams
Windsurf
AI-native editor with Cascade (agent) feature
Focus on autonomous multi-file editing
Growing ecosystem and model support
CLI tools
Claude Code
Terminal-based AI with full project file access
CLAUDE.md project memory files for persistent context
Pipe integration for CI/CD and scripting workflows
/plan command for read-only analysis before execution
Available as local CLI and cloud-based (Claude Code on the Web)
Gemini CLI
Google terminal AI tool
Strong integration with Google Cloud services
Similar workflow to Claude Code
Codex CLI (OpenAI)
OpenAI terminal-based coding tool
Sandboxed execution environment
Cloud/async agents
Claude Code on the Web—Sandboxed Claude Code execution in a cloud container
GitHub Copilot Coding Agent—Assign issues to Copilot, get PRs back
Google Jules—Google's cloud-based coding agent
OpenAI Codex—OpenAI's cloud coding agent with sandbox
Cursor Background Agents—Cloud execution from Cursor
Choosing your tools
For most developers starting out, we recommend: VS Code with Copilot or Cline as your AI editor, plus one CLI tool (Claude Code or Gemini CLI). This combination covers the vast majority of use cases. Add cloud agents once you're comfortable with the fundamentals.
Reference: Model Comparison Guide
Claude, GPT, Gemini, DeepSeek—capabilities, pricing, and context windows
Different AI models have different strengths for coding tasks. This guide helps you understand the landscape.
Claude (Anthropic)
Models: Claude Opus 4, Sonnet 4, Haiku 4.5
Strengths: Strong reasoning, excellent at following complex instructions, long context windows (up to 200K tokens)
Used in: Claude Code, Cursor (optional), various third-party integrations
Best for: Complex code generation, architectural planning, detailed code review
GPT-4o / GPT-4.1 (OpenAI)
Models: GPT-4o, GPT-4.1, o3, o4-mini
Strengths: Strong general-purpose coding, fast response times, broad knowledge base
Used in: GitHub Copilot, ChatGPT, Codex
Best for: General coding tasks, quick prototyping, broad language/framework coverage
Gemini (Google)
Models: Gemini 2.5 Pro, Gemini 2.5 Flash
Strengths: Very large context windows (up to 1M tokens), strong at code understanding
Used in: Gemini CLI, Google AI Studio, Cursor
Best for: Working with very large codebases, projects requiring extensive context
Open-weight models
DeepSeek Coder, Qwen Coder, and others
Can be run locally for privacy-sensitive work
Improving rapidly but generally behind frontier models for complex tasks
Practical advice
The model matters less than how you use it. All the techniques in this course—specific prompts, rich context, review-iterate workflows—work across models. Start with whatever model is default in your chosen tool and experiment from there.
Guide: Setting Up Your AI Coding Environment
Step-by-step installation and configuration for VS Code + Copilot/Cline, and Claude Code
This guide provides detailed setup instructions for the tools used in this course. Follow the section that matches your preferred toolset.
Prerequisites for all setups
A computer running macOS, Windows, or Linux
Node.js version 18 or later installed (download from nodejs.org)
Git installed and configured
A GitHub account (helpful but not required for all tools)
Setting up VS Code with an AI Assistant
Download VS Code from code.visualstudio.com.
Run the installer for your operating system.
When prompted, import your VS Code settings and extensions (optional but recommended if you use VS Code).
For Cline (free, open-source): search for "Cline" in Extensions, install, and configure your API key.
Open a project folder to verify everything works.
Test AI features: press Cmd+L (Mac) or Ctrl+L (Windows/Linux) to open the chat panel.
To configure AI models: Copilot uses built-in models; Cline lets you select from multiple providers.
To set up project rules: create a .cursor/rules/ directory in your project root and add .md files with your conventions.
Alternative AI-Enhanced Editors
Open VS Code.
Go to Extensions (Cmd+Shift+X / Ctrl+Shift+X).
Search for "GitHub Copilot" and install it.
Sign in with your GitHub account when prompted.
Test inline completion: start typing a function and watch for ghost text suggestions.
Test chat: open the Chat view in the sidebar.
For agent mode: select "Agent" mode in the Chat view dropdown.
Setting up Claude Code
Ensure Node.js is installed: run "node --version" in your terminal.
Install Claude Code: npm install -g @anthropic-ai/claude-code.
Navigate to a project directory: cd your-project.
Start Claude Code: claude.
Follow the authentication prompts (you'll need an Anthropic account).
Test by asking: "What files are in this directory?"
Create a CLAUDE.md file in your project root with your project conventions.
Verification checklist
Editor AI chat panel opens and responds to prompts
Inline code completion shows ghost text as you type
CLI tool starts and can read your project files
You can run code and tests from within your setup


Writing your first AI-generated code
Selecting transcript lines in this section will navigate to timestamp in the video
Time to get practical. Let's write real code with AI from scratch. I want you to see the full process, not just the polished end result, but the conversation that gets us there. I'm over here in VS Code, and I'm going to build a simple but useful utility, a URL shortener API endpoint. Now, this is something real that exercises several skills, string handling, storage, routing, and validation. So I've opened up the agent mode in VS Code, and you'll see that I have a short spec here. You know, it says create a simple URL shortener module in TypeScript. So I specified the language, and I mentioned this should have three functions, shorten URL, expand URL, and is valid URL. And I wanted to use an in-memory map for storage. Now, what I could do here is I could go ahead and just give this prompt to the agent to begin implementing. What I would actually like to show you is how you can pair this with plan mode. So in that agent dropdown, as we've seen before, you've got a few options. You've got agent, ask, and plan. And if I give this shorter spec to my plan mode, the model can actually generate a much more robust and comprehensive version of this. So here you'll see it's defining the requirements. So it's initializing the design or it's refining the implementation. And this is effectively writing me out a full product requirements document. So here's the plan. You'll notice much more detailed, a lot more nuanced than what I'd started out with. And then we can just click start implementation here, which will begin implementing this using that plan. So it's creating some to-dos, it's evaluating what it needs to get done here, and you will begin to see the diffs appear in your screen. So I'm gonna keep these changes. you'll see that our basic implementation is starting to come together here. That includes the shortened URL for generating a short code and storing the mapping, our expand URL for looking up the original URL from a short code, and is valid URL for validating that a string is a properly formatted URL. So you'll also notice we've got our JSDoc comments here. It's going to handle edge cases like invalid URLs and duplicate entries. Now, notice how I structured that prompt that I originally gave it. I asked for the language, TypeScript. I asked for the specific functions that I wanted with names and signatures, as well as the storage mechanism, an in-memory map, and the extras, things like JSDoc comments and the constraints, like the edge cases that I wanted. Now, the agent has gone and it's generated the code. That specificity, that time I spent with the planning, it makes a huge difference. So let's see what we got. You'll notice that we've got our summary here with everything that it was able to generate. The agent created a URLShortener.ts file here, what we're looking at, and I can walk through it. I can see that it can import the right modules for validation. We've got that isValidURL function that has a try catch that's able to use like a nice standard approach for how we want to do this. We've got a shortened URL function for generating a random code, checking for duplicates, storing in the map. And we've got our expand URL function that does lookup with proper error handling, as well as our JSDoc comments on each function. Great, this is a solid first pass. The AI got us to a working implementation in about 30 seconds. But, and this is important, we're not done. This is where the real workflow begins. I can already see a few things that I'd want to improve. Maybe the random code generation could produce collisions with enough entries. There's no URL normalization in place. So if I typed in, you know, the same URL with a backslash or in capitals, it would probably create different entries. And even the error handling could throw generic errors instead of custom types. But that's exactly what we want for a first draft. We have the structure, we got the logic, and now we can refine. That's the workflow. Get a solid first draft done, then iterate with precision. So pro tip, when asking AI to generate code, include the function signatures in your prompt. Saying a function called shorten URL that takes a string and returns a string gives the AI a concrete target. Without signatures, you'll get whatever the AI thinks is appropriate, which may not match your interface.
The review-iterate workflow
Selecting transcript lines in this section will navigate to timestamp in the video
We have our first draft of the URL shortener. Let me show you the most important workflow in AI-assisted development, the review iterate loop. This is where the real value happens. So we're back in VS Code here, and this is the code from our last video. I identified a few things we could improve. So let's work through them one at a time. Don't ask the AI to fix everything at once, that's a key point. We're going to do small focus iterations to produce better results. So the first thing I want to do is introduce collision-resistant shortcodes. So I'm asking the AI, the shortened URL function uses math.random for code generation. This could produce collisions at scale, refactor it to use crypto.random uuid. And this is going to just really help us to make this a little bit more robust. So it's updating the code generation right now. It's investigating what crypto libraries it should be using, what built-in APIs are available. and just watch what the model is actually doing as it thinks. It's trying to decide what dependencies should be installed. It's trying to look at does this actually work. Once again, you'll notice that it's trying to install some dependencies so we can allow that. In this case, this is a TypeScript project, so it's trying to install those types. It's running its build. It's noticing maybe there are some things that are still missing. And again, really, really good to be paying attention to how it's doing its workflow so that you can notice if there's anything that's gone wrong, off the rails, needs correction, or just to like build up your understanding of the implementation. So here we're beginning to see the diff and in fact if you look at the diff that's getting generated it's now completed those changes and it's replaced that you know some of our math.random functionality with what I asked for and it's added a while loop to handle our collisions. Now, let's review this carefully before accepting it. Maybe the while loop has got a few retry checks. That's good, it won't necessarily loop forever. The UUID approach gives us much better randomness, and I'm going to accept that change. Now, the next thing that I want to do is look at URL normalization. And so what I'm going to ask the model is to add URL normalization to shortened URL so that equivalent URLs like example.com and example.com with a slash still produce the same short code. I want to normalize that by removing those trailing slashes and lowercasing the host name. Okay, so as we can see, once again, it's thinking, it's integrating the normalization logic. You can go back and read through all these notes as it's doing the work for you, but it's integrating that robust approach to normalizing URLs. It's working to maintain some of that logic structure that we have. It's adding some additional checks in place, once again, to make this really robust, optimizing the URL normalization now that it's got a version of this that it's happy with. And eventually we will get to see that diff and the changes that it made, but it's still just working through this right now. It's also noticed that it has to make some other changes to how it might approach credential handling, the regex, parsing URLs. And once again, reading through this, very, very helpful. It's analyzing the regex behavior. It wants to make sure that this continues to be as flexible as possible for the input. It's going to be testing those changes out as well. We can see it's refining the regular expression, validating edge cases. Now this is how it's kind of thinking as a senior engineer. It's thinking through like what are the different ways that this use case might be approached? What do the tests need to be expanded to in order to make sure that this is actually going to provide the right output and you'll notice like if you take a look at some of those like it's got the capital example.com lowercase, the one with the slashes our tests are actually doing a lot of what we need and now we're starting to see our diffs good it's now going to complete this in just a moment but we'll have it normalizing before storing and i'm just waiting for it to complete now it's clarifying our test scope. It's running some last checks identifying the final implementation, doing some more reconciliation. It's running our npm build, and we're done. Amazing. So this version strips all the trailing slashes from the URL using a regular expression, it parses out the hostname, and it lowercases the hostname before storing the mappings. I want to emphasize how important it is to just read the summary of the changes so that you know for the next time you make some changes. So we've got our trailing slashes removed, hostname lowercase, and the normalization happens. The next thing we're gonna take a look at is custom error types. So I'm gonna replace the generic errors that are thrown with custom error classes. So I'm gonna use a prompt that asks for URL not found error to be used for missing codes, and invalid URL error for bad URLs. And I want these to be defined at the top of this file. So we have our prompt here, we're sending it to the model, and it's now gonna begin working. So it's implementing our error classes, it's enhancing the error reporting, refactoring some of those error throws, and you're already gonna see the diff here. Do you see that? We have those at the top of the file, it's already updated and it's done. We now have specific error types that consumers of this module can catch and handle differently. This is much better for a production code base. And so we have our final output here that you can take a look at. We started with a solid first draft and we made three targeted improvements. The code is now more robust. We've got collision-resistant codes. It's more correct. URL normalization prevents duplicates and it's more maintainable. Custom error types give us better error handling. This is the review iterate loop in practice. Review the generated code with a critical eye, identify specific things to improve, and request the change with a clear, focused prompt. Notice how we did those very small chunks, those specific changes, rather than asking for a bunch of things to be changed at once. Finally, review the diff before accepting, rinse and repeat. Each cycle is a small, focused, and verifiable change. You always know what changed and why. This loop is your bread and butter. The key principles here are one change at a time. Don't handle multiple files in a single request with lots of different changes at once. Review every diff. Read what changed before accepting it. And commit after each good change. Small commits are your safety net. Start a new chat if the context gets muddled. And long conversations can degrade AI quality, so just keep that in mind. One more critical principle, treat every piece of AI-generated code as a helpful draft that must be verified. If your pull request doesn't contain evidence that it works, you're not shipping faster, you're just moving work downstream. This means proof over vibes. Don't just trust that AI code works because it looks right. Run it, test it, click through the UI. Before you commit, make sure you can answer, how do I know this works? Here's a pro tip, after each iteration, commit your changes with a clear message. Something like git commit feature, add collision-resistant code generation. These small commits mean you can always roll back one specific change if a later iteration causes problems. Think of commits as safe points in a game. Save early, save often.
When AI gets it wrong
Selecting transcript lines in this section will navigate to timestamp in the video
AI tools produce plausible looking code with complete confidence, including when it's wrong. Let me show you the common failure modes and how to handle them, because this will happen to you at some point. So we're going back to VS Code, and I'm going to intentionally ask the AI to do something where it's likely to stumble. So I've asked it to write a function that uses the Web Streams API to process a large CSV file in chunks, parsing each chunk and yielding rows as they become available using ReadableStream and TransformStream. So this is the output that it's given me. It's created a CSV transform class. It's got all of this functionality in here, but you dig a little deeper and there's a few different problems here. At first glance, the implementation looks okay, but there's actually a few failure modes. So let me check a few things. Failure mode number one is hallucinated APIs. You see this right here. The API is calling through to stream.pipeThrough and ReadableStream.from in ways where it's actually not giving me options that exist in the actual spec. So this is a hallucination. The AI is generating code that looks like it should work and it uses naming conventions that make sense, but the actual API doesn't have those parameters. So how do I know that? Experience, experience with the API. But we can also verify this. So let me ask the AI directly, Are you sure about the parameters to pipe through? Show me the MDN documentation signature for this method. So what it's doing right now is it's going and it's fetching that documentation. It's going to read through it, double check the logic of what I've written up on screen here against that. And notice what it says. It says, you are correct to be suspicious. The parameters passed through are hallucinated and they don't actually exist in the actual Web Streams API specification. The options object only accepts these properties. And so my intuition there was actually right. So it's now acknowledged the correct signature is simpler. And this is a critical technique when something looks off, ask the AI to justify or cite its usage. Now, the next thing that we can do is take a look at outdated patterns, right? So now that I know there's a problem here, I can say convert this to use the newer pattern from ReadableStream.from for creating the stream. Now, in this case, what it's gonna do is it's gonna study my usage of this, check it against the documentation, check it against its own training knowledge, and try to give me a sense of, hey, is there a change that's actually required here or not? Now, there's some nuance, right? So the AI just used ReadableStream.from, but ReadableStream.from has limited browser support and the exact API might differ from what the AI has trained on. So it needs to go do some additional checking. So the line that I selected, it looks like it's already using the newer readable stream.from pattern, great. But it mentions to me that if I meant to convert it to the older, more widely compatible pattern instead, here is what that code would look like. So again, it's providing me this nuanced perspective. If I want broader compatibility, I would use this version. Otherwise, maybe I'm okay. So a pro tip, when AI uses an API, you're not 100% sure about, don't just ask the AI to confirm. It'll often confidently confirm its own hallucination. Instead, check the actual documentation yourself. Open MDN, open the official docs, take a look at them or ask it to do it itself. Now failure mode number three is subtle logic errors. So if we take a look at the CSV parsing logic, there's some problems here. It's not perfect. The parser splits on commas. But what about quoted fields that might contain commas? The AI generated the obvious approach that works for simple cases, but it breaks on real world CSV data. And that isn't a hallucination, it's a genuine logic gap. So the code compiles, it runs, and maybe it even passes those basic tests, but it's wrong for production use. This is the kind of bug you'll only catch if you think critically about the edge cases, which is exactly that 30% we talked about earlier. So let me catalog the failure modes for you so you know what to watch out for. Number one is hallucinated APIs. So functions, methods, or parameters that don't exist. If it looks right and it compiles, maybe it's okay, maybe it fails at runtime, just pay special attention to those cases. Number two is outdated patterns. So code from an older version of a library or a deprecated approach, pay special attention to those. Number three is subtle logic errors, code that works for the happy path, but can fail on edge cases. Number four is security gaps, missing input validation, SQL injection vulnerabilities, or improper authentication checks. Number five is confidence in correctness. So the AI saying something that's wrong, but feeling like it's right. Never mistake confidence for correctness. So how do you defend against these? Well, the first thing you can do is read every line. Don't just skim the AI output. Pay attention to the details. The next thing you can do is test those edge cases. So what happens when you've got empty input or malicious input or huge input? Also verify APIs. Check the documentation for unfamiliar methods and challenge the AI. Ask it, are you sure about this? Because it can often self-correct. Run the code, compile it, test it, break it. Do all of those things before you commit to your repo. A pro tip. When you catch the AI making an error, that's actually a big learning opportunity. Understand why it failed. That's going to teach you about the boundaries of AI assistance. Each failure mode teaches you to write better prompts and review more effectively next time.
Crafting effective prompts
Selecting transcript lines in this section will navigate to timestamp in the video
The quality of AI-generated code is directly tied to the quality of your prompts. Let me prove it with a dramatic side-by-side comparison. So we're back in VS Code, and I'm going to ask the AI to build the same thing twice with two very different prompts. So the task is a rate limiter for an API. We're starting off with make a rate limiter, which is a very high-level prompt. This is more like vibe coding. I'm deferring to it to figure out the best way to approach this problem. this problem, if it needs to install dependencies, I'm okay with that, I just want a solution that works. Now this may not be perfect, but what I'm expecting is for it to make a basic function that counts requests. So it's still reasoning through the files that it needs to write, it's defining the scope here, it's following a refinement process, it's trying to figure out the best way to approach this, and keep in mind that a rate limiter is gonna have a few different pieces of functionality. So let's see. It looks like it's got an implementation, so let me look at this more carefully. OK, we got something. It made a basic function that counts requests. Now let me look at this. It uses what looks like a simple counter and no per-user tracking. It's not necessarily thread safe. Now this works for the simplest case, but it's not necessarily what we would ship to production. Now let me try this again with a better prompt, something much more specific. We're gonna ask it to create a rate limiter in TypeScript with these requirements. I want a sliding window algorithm, so not a fixed window, per user tracking, using a user ID as the key, configurable limits, so requests per window with a window size in seconds. I want it to return an object, so we've got a specific signature in mind here. I want it to handle the cleanup of expired entries to prevent memory leaks, include type definitions, and I've just sent this to the model. So it's now developing out a better rate limiter. And we've given it these constraints, right? So we're gonna have proper TypeScript interfaces for our config and our response. We're gonna have a sliding window rate limiter as well with configurable settings and per user tracking with map-based storage and a clean API, public API that has the exact shape that I want. So it's now building this out. It's still working. It's cleaning up our interval. It's evaluating that the implementation is matching what my original requirements were. And soon it should be done. So it's updated the rate limiter now. We have that sliding window algorithm. Memory leak is prevented. Type definitions are in there. Now this is the same AI and the same model. It's just dramatically different output. And the only variable was the prompt. Here's what makes a prompt effective for code generation. First, language and environment. I said I wanted this in TypeScript. You can specify if you want it to work with Python, react with hooks, any of those specifics. Next, what specific functions or class names do you want? Tell it what to call things. Then you've got your input and your output contracts. So I take X and I return Y. You've also got your algorithm or your approach, like use a sliding window or use recursion. You've also got constraints, like handle the cleanup, be thread safe, or no external dependencies. And you've also got your edge cases, so handle empty input or handle concurrent access. You don't need all of these for every prompt, but the more specific you are, the better that first output. Think of it as a formula. Task plus context plus constraints plus output format equals better code. So the task is what do you want? Create a rate limiter. Context is the technical details. So TypeScript, sliding window algorithm. Constraints are the boundaries, like per user configurable, memory safe. And the output format is what it should look like. So what do you want me to return? Here's a pro tip. One technique that's surprisingly effective is giving the AI a function signature and asking it to implement it. For example, implement this function that looks this specific way, takes a specific set of inputs, and returns a very specific response. Starting from a signature gives the AI a precise target to hit. It's like giving somebody a blueprint instead of a wish.
Common prompting patterns
Selecting transcript lines in this section will navigate to timestamp in the video
Beyond writing specific prompts, there are repeatable patterns that consistently improve AI code output. Let me show you the three most useful ones. So we're going to switch over to Claude Code. So here I am in Claude Code right now, and I'm going to show you these patterns working across tools, not just in editors. The first pattern I'm going to show you is Chain of Thought. Now, Chain of Thought prompting basically asks the AI to reason through a problem step-by-step before writing the code. And this is especially powerful for complex logic. And so we're going to use Claude Code here, and I'm going to tell it I need to implement a function that determines if a scheduling conflict exists between a list of calendar events. Before writing the code, think through the algorithm step-by-step. So I'm pasting that in to Claude Code, and what I want it to show me is what data structure represents an event, how should events be sorted for efficient comparison, what constitutes an overlap, what edge cases exist, like same start time or adjacent events or all day events, and then I want you to implement the solution. So here, Claude Code has done its work, and it's told me the data structure and event, like an event starts with a start and end time, what is the sorting that it's going to be using, what constitutes an overlap, as well as edge cases like the same start time. This is great. So I now have this, and it's already begun to implement this. so you can see the diffs here right on screen. Claude first reasoned through each question, it defined the data structure, it explained the sorting approach, clarified the overlap detection, identified edge cases, and then it began to write the code. The result is significantly better. Really, by forcing the AI to think before it codes, we got more thoughtful implementations out of this. It's great stuff. Next up, we've got few-shot examples. Now, few-shot means giving the AI examples of the pattern you want, then asking it to continue. So for example, here's how we define an API endpoint for handlers in this project. Example could be get a certain endpoint, export, create the handler. We pass it a bunch of options. And really, this just provides a clear set of guidelines and guardrails around what this implementation is going to look like. We're being very specific. And I want it to follow an exact set of patterns for posting events and so on. I've given it this specification now, and I want it to follow this exact pattern for all of those different endpoints. So we've given it the spec, it's now beginning to think through, and let's see what happens. So we're beginning to get our diff here, I'm just allowing it to continue moving forward. And so let's see, it's still reasoning through. Occasionally, if you run into errors that's totally okay. I'm just allowing it to continue moving forward. And so we see that all four handlers are implemented using this exact pattern. All right, and so what we can see here is that it used Zod Schemas for validation and matching the naming convention. Each of them is working exactly the way that we would expect. Perfect, so the AI matched our pattern precisely. Same structure, same naming convention, and correct Zod schemas. By showing it one example, we got four consistent handlers. Without the example, we might have gotten four very different styles. So this already has a lot of value. Now we're now gonna move on to pattern three, which is role and constraints prompting. This pattern sets up the AI with a very specific perspective and boundaries. In this case, we're gonna focus on security. So act as a security-focused code reviewer, review the following authentication function and focus specifically on token validation and vulnerabilities, timing attack risks, information leakage and error messages, as well as the top 10 relevant OWASP issues. Don't suggest stylistic changes, only flag security concerns with severity ratings, so critical, high, medium, and low. So by giving the AI a role like security-focused reviewer and explicit constraints, like only security, no style, we got a much more focused and useful review. So there are three patterns to remember, and we're gonna look at these through pattern, when to use, and key phrase. So chain of thought is great for complex logic and algorithms. It's useful when you have to think step-by-step before coding. Few-shot examples is useful for style matching and consistency, like here's an example, follow this pattern. Role and constraints is useful for reviews and specialized tasks, such as act as a particular role and focus on a particular scope. These patterns work well with every major AI tool. So VS Code with Copilot or Cline, Cursor, Claude Code, any of those tools. Here's a pro tip. You can combine these patterns. My most effective prompts for complex tasks use all three, like act as a back-end senior engineer that does this. Think through the data flow step by step, chain of thought, this is very useful. And then you can say here's how our existing endpoints look, give it a few shot, and then build a new endpoint. So combining patterns isn't overkill, it's precision.
From prompt to production
Selecting transcript lines in this section will navigate to timestamp in the video
Let me now tie everything together and show you the complete workflow, from initial idea to production ready code. This is the process that I use daily. So we're over in VS Code and we're going to build a real feature, an API rate limiter middleware for Express. Not just the rate limiting logic, but the full middleware with logging, headers, and configuration. So we're going to start off with a spec. Before I even open up the AI, I'm going to write a brief spec. And here's the key insight, you don't need to write an RFC-length document. You can start with a high-level vision and let the AI help fill in the details. You can use your plan mode, you can spar with it, you can ask it questions, and work together to build up that plan. So I'm going to create a file now called spec.md. So this has got my spec, it's got my goals, my requirements, my interface, and my constraints. So my goal is Express Middleware that rate limits API requests by IP address with proper headers and monitoring. We've got our requirements in here, so again sliding window algorithm, configurable, request for window, window size, returns 429s, it adds x-rate limit remaining and x-rate limit reset headers, skips rate limiting for health checks, and we've got our interface as well. So I have now given the model this spec and I've said read spec.md and implement the rate limit or middleware exactly as specified. So it's already initialized the task and it's gone now and it's implemented this for us. Now this takes 60 seconds to write and it transforms the AI's output quality. Notice the structures. We gave it a clear goal, specific requirements, an interface contract, and constraints. And it's just got this done. So you plan first, then you execute. Tools like Claude Code offer a plan mode that restricts the agent to read-only operations. It can analyze your code base and create detailed plans, but won't write any code until you're ready. Step two is generate the first draft. I'll feed this to the AI, so like read that spec, implement the rate limiter and you get your output. Step three is review and iterate. So you want to open the generated files and actually review them. The first draft might look good. Let me check this against our specs. So we have our sliding window here. Looks good. Is it configurable? Yes. We've got 429 responses with headers. Yes. It's also got that. Is it skipping the paths correctly? Yes. What about logging? There's some logging in there as well. Awesome. Now I also want to make it configurable. All right, so let me make that improvement. We're going to refactor the logging to accept an optional logger function in the config instead of using the console logging directly. Default to console.warn if no logger is provided. So we're going to submit this to the model now and it's going to start making those changes. Awesome. So it's already starting to edit our implementation and it's refactored the logging implementation. Awesome. Now for step four we can see that this is already done and we can quickly verify. One of the things I love about VS Code's implementation here is you can sort of hover over some sections here and jump right into the changes. Now for step four what we're going to do is we're going to add tests. So I'm going to use another prompt here. I'm going to write write comprehensive tests for this middleware using Jest and SuperTest cover these cases. So we're just going to paste that in to the VS Code chat and the model is going to start reasoning through this and what I want this to cover is sort of those requests under the limit that pass through requests over the limit get a 429 rate limit headers are present on all responses skip paths that are not rate limited and we want that window to reset after the configured time. So the agent is now working through, it's defining the test scope, it's trying to look at all of those requirements and make sure that they are met, it's created our rate limiter.test.ts file, it's generating the updated package.json as well as our jest configuration, and it's now just making sure that it has everything it needs. We've allowed it to start making those dependency changes. So it's written our Jest config. It's updating our TypeScript configuration. It now wants to run those tests that it just wrote just to check if everything is correct and in case anything else needs to be updated. So it's still working. This is great, we can see the progress. Now I love that it actually proactively runs the tests for you and it starts to check if there's anything else that needs to be done. Perfect, so it looks like our tests are passing. Let me commit this. So I'm going to use Git. I'm just going to add these files and commit them. And here's the workflow that we just followed. We started off with a spec. I wrote a clear spec before touching the AI. That took me maybe 60 seconds. I generated. I fed the spec to the AI for a first draft. I then reviewed. I checked the output against my spec line by line. And then I iterated. I fixed specific issues. We targeted prompts. I tested, I generated, and I ran tests to verify the behavior, and I committed. I saved my work with a clear message. This is AI augmented software engineering, not just AI automated. You directed every step yourself. Now here's a pro tip. Save your specs. Keep a specs directory in your projects. When requirements change later, I update the spec and I ask the AI to update the code to match. You'll get, you know, version history, which is great. And the spec becomes your source of truth, not the code.
Deep Dive: Prompt Engineering Techniques for Code
Advanced techniques beyond the basics
This article explores advanced prompt engineering techniques for AI-assisted code generation, going beyond the patterns covered in the video lessons.
The prompt engineering spectrum
Prompt engineering for code exists on a spectrum from simple to sophisticated:
Zero-shot: "Write a function that validates emails."—No examples, no structure
Structured: Task + context + constraints + output format—The approach taught in this course
Few-shot: Providing examples of the pattern you want—Covered in the Common Prompting Patterns video
Meta-prompting: Asking the AI to help you write the prompt—Advanced technique
Multi-turn orchestration: Breaking complex tasks into a series of coordinated prompts—Expert technique
Meta-prompting
Meta-prompting is asking the AI to help you write a better prompt before generating code. Example:
I want to build a rate limiter. Before you write any code,
ask me 5 clarifying questions that will help you generate
a better implementation.
The AI might ask about: algorithm choice, storage backend, configuration needs, error handling approach, and testing requirements. Answering these questions gives the AI much better context for code generation.
Structured output prompts
When you need code in a specific format, be explicit about the structure:
Generate a TypeScript module with:
1. A type definition for the config
2. A class that implements the logic
3. A factory function that creates configured instances
4. Named exports (no default export)
Do NOT include example usage or comments explaining the obvious.
Common anti-patterns to avoid
Over-prompting: Adding so much detail that the prompt itself becomes confusing. Keep it clear and scannable
Under-constraining: Not specifying what you DON'T want. The AI will fill in the blanks with assumptions
Copy-paste prompting: Using the same prompt template without adapting it to your specific context
Prompt chaining without review: Sending the output of one prompt directly into another without human verification
Ignoring the context window: In long conversations, the AI loses track of earlier requirements
Tips for continuous improvement
Save prompts that work well in a personal library
When a prompt produces poor results, analyze why before rewriting it
Start simple and add specificity only where needed
Use the review-iterate workflow: generate, review, refine the prompt, regenerate
Template Library: Prompts for Common Coding Tasks
Copy‑paste prompt templates for everyday AI‑assisted development. Replace the [bracketed placeholders] with your specifics.
Code generation
Create a [module/class/function] in [language]:
- Name: [specific name]
- Input: [input type and description]
- Output: [return type and shape]
- Algorithm: [approach to use]
- Constraints: [limitations and requirements]
- Edge cases: [specific scenarios to handle]
Include type definitions and JSDoc/docstring comments. 
Bug fixing
I have a bug in [file/function]:
Error: [paste error message]
Expected: [what should happen]
Actual: [what happens]
Context: [relevant code or @file references]
Find the root cause and suggest a fix.
Do NOT change unrelated code.
Refactoring
Refactor [function/class/module] to:
- [Specific improvement]
- [Constraint: preserve public API]
- [Quality goal]
Do NOT:
- Change function signatures
- Add new dependencies
- Modify test files
Test generation
Write [framework] tests for [module/function]:
- Happy path with typical input
- Edge cases: [empty, null, boundary values]
- Error cases: [invalid input, failures]
- [Domain-specific scenarios]
Mock: [external dependencies]
Structure: [describe/it blocks or test classes]
Code review
Review this code for:
1. Correctness — logic errors, edge cases
2. Security — injection, auth, data leakage
3. Performance — bottlenecks, memory
4. Maintainability — clarity, structure
Rate issues by severity.
Do NOT suggest style changes.
Documentation
Write documentation for [module/API]:
- Overview: what it does and when to use it
- API reference: each public function/method
- Examples: 2-3 usage examples
- Error handling: what can go wrong
Format: [Markdown/JSDoc/docstring]
Audience: [developers on the team]
Performance optimization
Analyze [function/module] for performance:
- Identify bottlenecks
- Suggest optimizations with trade-offs
- Estimate impact (high/medium/low)
- Preserve correctness
Context: [expected data volume, frequency of calls]
Why context beats prompts
Selecting transcript lines in this section will navigate to timestamp in the video
We've covered prompt engineering, crafting what you ask. Now let's talk about something even more impactful, context engineering, controlling what the AI knows when you ask it. So here's a claim I'll prove right now. The same prompt with different context produces dramatically different code. Here on screen, I've got context engineering best practices from Anthropic, and there are a few different things that we mean when we say good context engineering. You might wanna include the task context, the tone, any important background data, documents, images, a detailed task description, examples, maybe some conversation history, and whether you wanna include any of those patterns we talked about before, such as thinking step-by-step. So we're now gonna move over back into VS Code, and I have an existing Express API project here. Let me show you the difference the context makes. So our experiment is the same prompt, but just two different contexts. Now context A is where no context is provided. We're opening up a new chat, and I've asked you to write an error handling middleware for Express. So there's no file selector, there's no additional context. I just want to show you the result. This is when we've kind of delegated to the model to figure something out. So what we get is a generic error handler. Maybe this catches errors and logs them. It's pretty generic. It uses, you know, returns a plain response. It doesn't really know anything about our project. And so it's a very simplistic implementation. Now, context B is where we provide more of that context that I was talking about earlier. I'll ask the same question, but I'll first add some context. Now, there's a few different ways that you can introduce context into your projects. One of them is to start tagging in additional files, requirements, examples, anything that you may have that can help the AI get guided towards that final outcome that you have in mind. So we can use a very similar prompt like write an error handling middleware for Express, but if there are specific files that you might want to attach, we're gonna include those in there as well. So here I'm going to start using, you can use the at symbol, you can use the hash symbol, you can include an entire directory of documents. You can include specific files in here as well for that context. And I just want to show you some examples really quickly here. I can include any of the files in my project as context and those will get put into the context window. So server.ts, for example. If I want to add other kinds of files, like maybe our type definitions, I can include those. And other kinds of files that might just enrich the context that the model has about how you want the problem to be solved. So we're just going to attach those final files. And now I'm sending my prompt to the model, write an error handling middleware for Express. And let's see how this output differs. So we're just saving a file for the scaffold to just do that right real quick. And off we go. All right, so now it's going to think through those files. It's defining the requirements. Still a high level prompt, but it's got that context. It's checking the contents of these files. It's trying to reason about, you know, how they fit into the bigger picture, how it should be influenced by them. It's generating the tool calls to figure out, okay, well, how do I connect all of this together to solve that problem in this context? And there are many ways that, you know, you can pull context in. Some people will pull context in from the web, from their local project, from their team directory. So here, it's decided to go and implement this. We've got custom types integrated. We see that we've got app error identification, structured logging. It's formatted all the JSON responses to use a standard approach. This is completely different output and it's dramatically better. It uses our custom app error class from our types.ts. It follows our existing middleware patterns from our existing middleware.ts. And it integrates with our existing logging setup instead of using something different. It also returns errors in the same JSON format as our other endpoints and handles the specific error types that our app throws. So what you're hearing is a lot more specificity and closer to what I myself would write because this is my team project. So same prompt, same AI model, the only difference was the context you provided. So the AI went from writing generic boilerplate to writing code that fits our actual code base. This is context engineering and it's the skill that separates beginners from experts. Now here's why context matters more than prompting for real work. A perfect prompt with no context is gonna produce generic code. A basic prompt with rich context produces tailored code. Think of it this way. If you hired a contractor and said, build a shelf, they'd build a generic shelf. But if you showed them the room, the existing furniture, the wood type, and the style, the same request would give you a much better result. Now, there are a few different types of context you can provide. Just to recap, there's code context, so existing files, functions, patterns in your project, that's super important. Then you've got documentation, so anything from your README files to your API docs, your architectural decision records, anything that your team might consider context you maybe commit to the same repo. Then you've got your constraint context, so what the AI should not do. And then you've got your example context, so sample inputs, outputs, existing implementations to mimic. Now, most developers focus only on code context. Using all four dramatically improves your results. So here's how these types rank and impact. Relevant code from your project has the highest impact. Constraints and requirements have pretty high impact. Examples have medium to high impact, and docs and architecture are sort of medium impact as well, depending on the kind of docs. Now, general instructions probably have the lowest impact, where most people spend all of their effort today. You might remember this called prompt engineering, and this is why we've shifted over to focusing more and more on context. The takeaway, spend less time crafting the perfect prompt, and more time curating the right context. As a pro tip, in VS Code and many other editors, you can tag in those files and all of your context with ease. In Claude Code and Terminal tools, you know, they can read your project automatically, but you can also guide them by saying, look at how error handling works in this particular directory or this particular file before writing it. Every tool has its own way of feeding context. Learn yours.
Feeding AI the right information
Selecting transcript lines in this section will navigate to timestamp in the video
Now that you understand why context matters, let me show you how to feed AI the right information. There are practical techniques for every tool. We're going to head back into VS Code and Claude Code. The first technique I want to show you is project memory files. The most powerful context technique is a project memory file, and you can use an agents.md file for this, you can use Claude.md, you can use project specific markdown files as well. Lots of different techniques for this. Now, what I want you to just keep in mind is that these files are generally loaded automatically in every conversation. You can either reference them automatically or reference them yourself directly. And I want to show you a Claude.md file that I would actually use in a project. So, I've got my Claude.markdown file open right now. And this project is for an e-commerce API. We've got our architecture here. We've got it using Express and TypeScript. We've got a Redis instance for caching and rate limiting, just for testing. We've got some conventions in here as well, like all endpoints follow a RESTful naming scheme. We've got using Prisma transactions for multi-table operations. All prices are stored as integers. We've also got some commands, some things that you would put inside your package.json, just so that you have a full tooling flow in there, as well as known issues. Now, known issues are something we're using for this demo, not necessarily something you would probably use yourself in practice. Now, we can actually get started using this. So I'm gonna say implement a caching layer for the user profile endpoint. Now, as we mentioned, this file, this agents.md file is gonna be loaded in every single time the AI starts a session. It's like giving a new team member the here's how we do things document. Now, as a pro tip, there's a powerful habit you can use. Every time the AI makes a mistake that your project memory could have prevented, add that to your files. You can have a lessons.md file, or you can include this in one of the other configuration files, but over time, your project memory becomes a comprehensive guide that prevents recurring mistakes. Now, I also wanna show you targeted context with at mentions and file references. Now, as I mentioned before, you can tag in specific files, directories, documentations, and in some cases, even full URLs that you want as context. So we're back in VS Code here, and I just want to demonstrate very quickly those at mentions again. I want to show you those in action. So here we're inside the chat, and I just want to show you once again, with that at symbol or the hash, you can include different kinds of commands. You can include different kinds of context, and this is just a way for you to enrich that context engineering aspect of what you're doing. So you can include folders, you can include docs, you can even include references to Git changes as context. For Claude Code, you can say something like, read the files in a particular directory before making changes, or even pipe through Git diffs and other kinds of changes that can be used as context. So the key principle is to give the AI specific files it needs for the task at hand. Just don't dump everything that dilutes the important context in your context window. Now, the next thing I wanna talk about is negative context. So what not to do. This is the most underused technique. Telling the AI what to avoid is just as powerful as telling it what to do. So in our VS Code, we've got a prompt here. We're gonna say, implement a caching layer for the user profile endpoint. And I've got it saying, don't use in-memory caching. You know, maybe I want it to avoid using specific kinds of dependencies. So reuse existing ways of doing things, change the existing endpoint signature and use cache aside pattern. Maybe there are specific design patterns I want to avoid. But I do want you to use the existing Redis client, set the TTL to five minutes, invalidate on user profile updates, and cache hit or miss to response headers for debugging. And so it's already thinking through this prompt, and it's going to take and keep in mind all of those do's and don'ts as it's building out the implementation. Now, because we're being very, very specific, this is gonna take a minute. There's a lot of logic here for it to process. Anytime it comes up with an implementation, it's also gonna have to keep in mind and double check that context and those guardrails that we've provided. So what I'm gonna do is just fast forward through this a little bit because this prompt does take a little bit of time to get done. And we're gonna take a look at the output. So it's now completed the implementation. And what it's built for us is we have our Redis for cross-server caching, it's installed the right dependencies, we've got a write-through pattern, It's got invalidation on updates in place, TTL constraints. This is a very nuanced implementation. Now, without the do not list, the AI might have installed a specific caching library, used in-memory caching, or picked a different pattern altogether. Now, external documentation is also really important as context, especially when you're working with specific libraries or frameworks. You see, AI models are often locked to a particular cutoff date for their knowledge in their training data. Now, let's say that we wanted to work with a particular payment provider's SDK, maybe Stripe or another payment provider. Now, you can either go to their documentation and copy paste references, so examples, API signatures, SDK signatures, any of that, or depending on the tool you're using, you might just be able to say, hey, I'm using this particular API, I want you to go and fetch the documentation to be used. The pattern itself, whichever one of those you're using, still looks about the same. So in this case, let's use this prompt. My payment provider's SDK probably has some docs, they are Stripe, can use their SDK and implement a function to process a refund. Now, alternatively, I can use a pattern that's just a little bit more generic and will apply to just about anybody. So the pattern that we're going to use here is here's the documentation for our payment provider's SDK. You can include the documentation, whether it's list of URLs or static docs that you've copied. Using this API, implement a function to process a refund. Now the AI works from real facts instead of guessing. And this is great. You're providing modern up-to-date guidance and that will increase the chances that you get the outcome that you're after. Now, by providing the actual documentation, you eliminate hallucinated APIs. The AI works from real facts instead of guessing. Now, here's a pro tip. A lot of libraries and frameworks, tech stacks, are starting to come up with their own sets of skills. So skills are like having an expert in a particular library or framework, and maybe that can point to specific documentation, specific tools that should be used, specific workflows, but they can often tell you exactly how to get a much better context-rich output. So keep an eye out for skills for those specific libraries and frameworks that you're using. For most more general tasks, even targeted mentions of specific files and local docs can work better than dumping in everything.
Deep Dive: Context Engineering—The New Superpower
Why context engineering matters more than prompt engineering
If there's one skill that separates highly effective AI-assisted developers from the rest, it's context engineering—the practice of controlling what information the AI has access to when generating code.
Why context beats prompts
A common misconception is that writing the perfect prompt is the key to good AI output. In practice, context matters much more. Here's why:
A perfect prompt with no context produces generic code that doesn't fit your project.
A basic prompt with rich context produces code that matches your patterns, uses your libraries, and follows your conventions.
Context reduces hallucinations because the AI works from real code rather than guessing.
Context is reusable across prompts—set it up once, and every prompt benefits.
The four types of context
Code context—Existing files, functions, types, and patterns from your project. This is the highest-impact context type.
Documentation context—README files, architecture docs, API documentation. This helps the AI understand why things are built a certain way.
Constraint context—What the AI should NOT do: "Don't add dependencies," "Preserve the API," "Use existing patterns."
Example context—Sample inputs/outputs, existing implementations to mimic, test cases showing expected behavior
Project memory files
The most powerful context technique is a persistent project memory file that loads automatically: 
Claude Code: CLAUDE.md in your project root
Cursor: .cursor/rules/ directory with .md rule files
VS Code + Copilot: .github/copilot-instructions.md
Cline: Custom instructions in settings
VS Code + Copilot: .github/copilot-instructions.md 
These files should contain: your architecture overview, coding conventions, common commands, and known gotchas. Think of them as onboarding documents for the AI.
Advanced context techniques
Negative context (DO NOT lists): Explicitly state what approaches to avoid. This is often more effective than just stating what you want.
Graduated context: Start with minimal context and add more if the output isn't good enough.
Context pruning: Don't dump your entire codebase. Select the 3-5 most relevant files for each task.
External documentation: Paste API docs directly into the conversation to eliminate hallucinated APIs.
Git context: Include recent git diffs to help the AI understand what's changing.
Building your context engineering habit
Create a project memory file for every project you work on.
Before each AI interaction, ask: "What files does the AI need to see?"
When the AI output is wrong, ask: "What context was it missing?"
Add recurring mistakes to your project memory file.
Share context strategies with your team.
Managing Context Windows Effectively
Strategies for large projects, long conversations, and staying within limits
Every AI model has a finite context window—the total amount of information it can process at once. Managing this window effectively is crucial for getting good results, especially on large projects.
Understanding context windows
Context windows are measured in tokens (roughly 0.75 words per token). Current model context windows:
Claude Opus/Sonnet: ~200K tokens (about 150,000 words)
GPT-4o: ~128K tokens (about 96,000 words)
Gemini 2.5 Pro: up to 1M tokens (about 750,000 words) 
This sounds like a lot, but it fills up quickly when you include: system instructions + conversation history + file contents + AI responses. In a long editing session, you can exhaust the window faster than you think.
Signs your context window is full
The AI "forgets" earlier instructions or requirements.
Responses become less relevant to your project.
The AI starts contradicting things it said earlier.
Code quality degrades in long conversations.
Strategies for managing context
Start fresh for new tasks: Don't continue a long conversation for unrelated work. Open a new chat.
Be selective with file references: Include 3-5 relevant files, not your entire project.
Use project memory files: They're loaded at the start of every conversation automatically, providing consistent context without manual management.
Summarize when conversations get long: "Summarize what we've done so far. I'm going to start a new conversation with that summary."
Chunk large tasks: Break big features into smaller pieces, each in its own conversation.
Commit often: When you commit working code, you create a save point. Start a fresh conversation for the next piece.
Working with large codebases
For projects with hundreds of files, you can't include everything.
Strategies:
Use tools like gitingest or repo2txt to create curated project summaries.
Include only the files directly relevant to the current task.
Rely on project memory files to provide architectural context without including all the code.
Use the AI's ability to read files on demand (Claude Code, Copilot Agent) rather than front-loading everything.
When the AI needs to understand a large file, ask it to read specific sections rather than the whole thing.
Key takeaway
Think of the context window as a precious resource. Every token spent on irrelevant information is a token that can't be used for relevant context. Be deliberate about what goes in, start fresh when needed, and use project memory files to maintain consistency across conversations.
Building your personal AI workflow
Selecting transcript lines in this section will navigate to timestamp in the video
We've covered a lot of ground, mindset, tools, prompting, context engineering, and the review iterate workflow. Now let's put it all together into a personal workflow you can start using tomorrow. Here's the workflow that I use right now. It's not the only way, but it's battle tested across dozens of projects. Phase one is plan. Write a brief spec or requirements doc. You can ask the AI to poke holes in your spec like what edge cases am I missing? and break the work into small, clear tasks. And there's phase two, build. You can feed your spec to your AI tool to generate code one task at a time. Review each diff before accepting it and make sure you're running tests after each change as well. You can commit after each working increment. That lets us move on to phase three, verify. You can generate comprehensive tests if you haven't already, run that full test suite and do a self-review. Is the quality good enough? Would you approve that pull request? You can even ask the AI to review the code from a fresh perspective. That lets us go to phase four, refine. You can address anything from the review, clean up any AI artifacts, like overly verbose comments or unnecessary code. And you can ensure the code matches your team's standards too. Finally, you get to commit and push. Now this whole cycle takes less time than it looks. The planning phase is measured in minutes instead of hours, and the build phase is dramatically faster with AI assistance. Now, your workflow should fit you. So here are some variations. If you're a terminal first developer, you can use Claude Code or Gemini CLI as your primary tool. Pipe everything in, your logs, your diffs, your error output, everything into the AI, and use the editor primarily for reviewing changes. If you're a visual developer, you can use VS Code with Copilot, Cline, Cursor as your primary tool, and you can take advantage of diff views and inline suggestions. Then use your agent mode for bigger tasks and chat for targeted edits. If you work on a team, you can use project memory files to encode your team's standards, use skills, you can use MCPs, run those AI-assisted code reviews on your pull request as well, and share your prompt templates and workflows with other members of the team too. Here's my advice on how to get started from scratch. So week one, use your inline completion. Just let the AI suggest as you type and get comfortable with tab to accept. On week two, you can start using the chat panel for targeted edits and questions. Ask it to explain code, fix bugs, and tackle some of that error handling. On week three, try agent mode for small contained features. Just give it a spec and let it generate the implementation. On week four, you can start writing your project spec files, handling your context, your memory, and really customizing the AI's behavior for your codebase. After a month, you'll have a feel for what works for you and where the tool adds the most value. Here's a pro tip. Keep a lessons learned note as you work with AI. You can capture what prompts work well, where the AI struggled, and what context you needed to provide. This personal knowledge really compounds fast, and in a few weeks you'll have an instinct for what to delegate to AI and what to handle yourself.
Next steps and resources
Selecting transcript lines in this section will navigate to timestamp in the video
You now have a solid foundation for AI-assisted development. Let me tell you what comes next. In this course, you've learned the mindset, how AI amplifies your skills and doesn't replace them. You've learned the tools, so editor, CLI tools, and how to choose between them. We've covered the workflow, so prompt, generate, review, iterate, test, and commit, as well as the techniques, so specific prompts, effective context, and handling AI errors, as well as the principles, like own your code, review everything, and commit often. These fundamentals apply to every AI coding tool you'll ever use. The specific tools will probably evolve, but these practices are durable. To keep learning, background reading material is linked throughout the course to go deeper on every topic. And my Substack covers AI coding developments as they happen. You've also got the tool documentation. And finally, practice. The single best way to improve is to use these tools daily on real projects. The AI coding landscape moves fast, but the engineering principles we covered, like plan, review, iterate, test, own your code, those aren't going anywhere. Thanks for learning with me. I'm excited to see what you build.

